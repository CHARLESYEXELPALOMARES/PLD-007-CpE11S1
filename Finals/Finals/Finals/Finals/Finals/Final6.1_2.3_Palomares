#include <iostream>
using namespace std;

const float exchangeRate = 50;  // Example exchange rate

float dollarsToPesos(float d) {
    return d * exchangeRate;
}

float pesosToDollars(float p) {
    return p / exchangeRate;
}

int main() {
    float dollars, pesos;
    cout << "Enter amount in dollars: ";
    cin >> dollars;
    cout << "Pesos: " << dollarsToPesos(dollars) << endl;

    cout << "Enter amount in pesos: ";
    cin >> pesos;
    cout
